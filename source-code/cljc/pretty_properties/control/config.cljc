
(ns pretty-properties.control.config)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @note (pretty-properties.accessories.config#4011)
; @note (pretty-properties.accessories.config#4012)
;
; @constant (map)
; {:href-target (keyword)
;  :href-uri (string)}
(def STANDARD-ANCHOR-PROPERTIES
     {:href-target :self
      :href-uri    nil})
