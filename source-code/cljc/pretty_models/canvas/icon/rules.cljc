
(ns pretty-models.canvas.icon.rules)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn icon-canvas-rules
  ; @description
  ; Applies the rule functions of the icon canvas model on the given property map.
  ;
  ; @param (map) props
  ;
  ; @return (map)
  [props]
  (-> props))
