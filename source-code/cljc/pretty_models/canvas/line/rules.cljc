
(ns pretty-models.canvas.line.rules)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn line-canvas-rules
  ; @description
  ; Applies the rule functions of the line canvas model on the given property map.
  ;
  ; @param (map) props
  ;
  ; @return (map)
  [props]
  (-> props))
