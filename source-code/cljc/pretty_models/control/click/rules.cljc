
(ns pretty-models.control.click.rules)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn click-control-rules
  ; @description
  ; Applies the rule functions of the click control model on the given property map.
  ;
  ; @param (map) props
  ;
  ; @return (map)
  [props]
  (-> props ))
