
(ns pretty-models.shape.attributes)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn shape-model-content-attributes
  [attributes _]
  (-> attributes))

(defn shape-model-inner-attributes
  [attributes _]
  (-> attributes))

(defn shape-model-outer-attributes
  [attributes _]
  (-> attributes))
